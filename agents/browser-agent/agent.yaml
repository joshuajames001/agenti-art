name: browser-agent
version: 1.0.0
category: browser
provider_config: ../../model_config.yaml

default_model: fast
allowed_models:
  - fast
  - smart

connectors:
  - browser
  - web-search

max_steps: 15
